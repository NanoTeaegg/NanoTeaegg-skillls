# 数据库设计文档(SCHEMA):<系统 / 模块名>

> 状态:草稿 / 评审中 / 已确认 · 最后更新:YYYY-MM-DD · 负责人:<填写>
> 上游依据:docs/PRD.md · docs/TECH_DESIGN.md(若存在,标注版本/日期)

## 1. 数据库选型与理由

- **选定产品:** <如 PostgreSQL 15>
- **类型与取舍:** <关系型 / 文档型 / KV;为什么是它而不是其他——结构化程度、事务/一致性要求、读写特征、团队熟悉度等>
- **关键能力依赖:** <用到的特性:JSONB、全文检索、行级锁、分区… 以及它们对应哪些需求>
- **若技术方案已定:** <沿用 docs/TECH_DESIGN.md 第 X 节的选型,本节仅作确认与展开>

## 2. 数据模型总览

- **核心实体:** <列出主要实体:用户、订单、商品…>
- **关系概览(文字版 ER):** 用箭头/基数描述实体间关系,例:
  - `User 1 ── N Order`(一个用户多笔订单)
  - `Order N ── M Product`(经由 `order_item` 中间表,带数量/单价)
- **命名约定:** <表名单/复数、命名风格(snake_case)、主键名(id)、外键名(<表>_id)、时间字段(created_at/updated_at)>

## 3. 表设计

> 按「领域/模块 → 表」分层。每张表必须写清:用途、字段、主键、关系、索引、约束/不变量。
> 字段类型用目标数据库的标准类型;DDL 仅作示意,不是迁移脚本。

### 3.1 <领域一,如「账户」>

#### 表:`<table_name>`

**用途:** <这张表存什么、对应哪个业务概念>

| 字段 | 类型 | 可空 | 默认 | 说明 |
|------|------|------|------|------|
| id | bigint / uuid | NO | | 主键 |
| ... | | | | |
| created_at | timestamptz | NO | now() | 创建时间 |
| updated_at | timestamptz | NO | now() | 更新时间 |
| deleted_at | timestamptz | YES | NULL | 软删除标记(NULL=未删) |

- **主键:** `id`(策略:自增 / UUID / 雪花——见第 1 节决策)
- **关系 / 外键:** `<fk_column>` → `<other_table>.id`,删除行为:`RESTRICT / CASCADE / SET NULL`(并说明为什么)
- **索引:** `idx_<table>_<col>`(用途:支撑 XX 查询);唯一索引 `uniq_<table>_<col>`
- **约束 / 不变量(可检验):**
  - <如:`email` 全局唯一且非空>
  - <如:`status ∈ {pending, paid, cancelled}`>
  - <如:`amount >= 0`>

> 示意 DDL(非迁移脚本):
> ```sql
> CREATE TABLE table_name (
>   id BIGINT PRIMARY KEY,
>   ...
> );
> ```

#### 表:`<another_table>`

<同上结构>

### 3.2 <领域二,如「订单」>

<同上结构>

## 4. 索引与性能设计

- **高频 / 热点查询:** <列出关键查询模式,及其依赖的索引>
- **复合索引与顺序:** <说明多列索引的列顺序及理由>
- **分区 / 分表(如需要):** <按时间/租户分区的策略与触发阈值;不需要则写"暂不分区,数据量级 < X 前无需">

## 5. 数据完整性与约束策略

- **外键与级联:** <整体策略:哪些用外键约束、哪些靠应用层保证,级联删除/置空的原则>
- **唯一性:** <全局唯一 vs 租户内唯一的字段清单>
- **校验约束:** <CHECK 约束、枚举值约束>
- **并发控制:** <需要乐观锁(version 列)或悲观锁的表与场景>

## 6. 敏感数据与合规

- **敏感字段清单:** <PII / 支付 / 健康等字段,所在表>
- **保护措施:** <列级加密 / 脱敏 / 仅存哈希;访问控制>
- **保留与删除:** <数据保留期;用户删除/导出(GDPR 等)如何在数据层实现>
- 若无敏感数据,明确写"本系统不存储 PII/支付等敏感数据"。

## 7. 变更与迁移规范

- **命名约定:** <见第 2 节,迁移文件命名、版本号规则>
- **迁移流程:** <怎么加列(默认值/可空先行)、怎么改类型、大表加索引(并发建索引/低峰执行)、能否停机>
- **向后兼容原则:** <如"先扩展后收缩":新增可空列 → 双写 → 回填 → 切换 → 删旧列;避免一步破坏性变更>
- **回滚策略:** <每次迁移是否可逆;不可逆变更(删列/删表)前的备份与确认要求>

## 8. 开放问题与假设

- **待确认:** <还没拍板、需要用户/干系人决定的数据建模问题——如数据量级、是否多租户、保留期>
- **假设:** <在没有确认前,本设计基于的假设——一旦被推翻需回头修订 schema>

## 9. 变更与迁移记录

| 日期 | 类型 | 内容 | 迁移要点(回填/可回滚/兼容) | 原因 |
|------|------|------|------------------------------|------|
|      | 新建 | 初版 schema | — | |
